# Supabase 配置详细指南（小白也能跟着做）

> 本指南面向第一次使用 Supabase 的用户，每一步都有详细说明。跟着做即可完成配置。

---

## 整体流程概览

```
1. 注册 Supabase 账号
2. 创建一个新项目
3. 执行建表 SQL（2 个文件）
4. 创建图片存储桶
5. 获取 API 密钥
6. 配置 .env 文件
7. 注册第一个账号
8. 把自己提升为管理员
9. 开始使用系统
```

整个配置过程大约需要 **10-15 分钟**。

---

## 第 1 步：注册 Supabase 账号

1. 打开 https://supabase.com/
2. 点击右上角 **「Start your project」**
3. 使用 GitHub 账号登录（推荐）或邮箱注册
4. 完成登录后进入 Dashboard 控制台

> 💡 Supabase 是免费的，免费版完全够你用（5GB 数据库 + 1GB 文件存储 + 5万月活用户）

---

## 第 2 步：创建新项目

1. 在 Dashboard 点击 **「New project」**
2. 填写项目信息：
   - **Name**：`inventory-app`（或你喜欢的名字）
   - **Database Password**：设置一个强密码（**务必记下来！** 后面登录数据库要用）
   - **Region**：选择离你最近的区域（如 `Southeast Asia (Singapore)` 或 `Northeast Asia (Tokyo)`）
   - **Pricing Plan**：选 `Free`（免费版）
3. 点击 **「Create new project」**
4. 等待 1-2 分钟，项目创建完成（状态栏会显示 "Project is ready"）

---

## 第 3 步：执行建表 SQL

这一步会创建所有需要的数据表（产品、仓库、库位、库存、记录等）。

### 3.1 执行第一个 SQL 文件（建表）

1. 在左侧菜单点击 **「SQL Editor」**（图标是个 `</>`）
2. 点击 **「New query」** 新建查询
3. 打开本项目文件：`supabase/migrations/0001_init.sql`
4. **复制全部内容**，粘贴到 SQL Editor 的输入框中
5. 点击右下角 **「Run」** 按钮（或按 Ctrl+Enter）
6. 等待执行完成，下方会显示 `Success. No rows returned.`
7. 这个文件会创建：
   - 6 张表：products, warehouses, locations, inventory, stock_moves, profiles
   - 自动创建用户的触发器
   - 入库/出库的事务函数

### 3.2 执行第二个 SQL 文件（权限策略）

1. 再次点击 **「New query」**
2. 打开本项目文件：`supabase/migrations/0002_rls.sql`
3. **复制全部内容**，粘贴到 SQL Editor
4. 点击 **「Run」** 执行
5. 这个文件会配置行级安全策略：
   - 所有登录用户可以查看产品/仓库/库存
   - 只有管理员可以增删改产品/仓库
   - 所有登录用户可以进出库操作
   - 员工只能看自己的操作记录，管理员能看所有

---

## 第 4 步：创建图片存储桶

这一步用于存储产品图片。

1. 在左侧菜单点击 **「Storage」**（图标是个文件夹）
2. 点击 **「New bucket」**
3. 填写：
   - **Name**：`product-images`（**必须完全一致**，区分大小写）
   - **Public bucket**：✅ **勾选**（设为公开，否则图片无法显示）
4. 点击 **「Create bucket」**

> ⚠️ 必须勾选 Public bucket，否则产品图片上传后无法在页面上显示

---

## 第 5 步：获取 API 密钥

1. 在左侧菜单点击 **「Project Settings」**（齿轮图标，通常在最底部）
2. 点击 **「API」**
3. 你会看到三个重要信息：
   - **Project URL**：形如 `https://xxxxxxxxxxxxx.supabase.co`
   - **anon public key**：一长串字符（公开密钥，前端使用是安全的）
   - **service_role key**：另一长串字符（**私密密钥，绝对不要放到前端！**）

4. 复制 **Project URL** 和 **anon public key**（只需要这两个）

---

## 第 6 步：配置 .env 文件

1. 在项目根目录找到 `.env.example` 文件
2. 复制它并重命名为 `.env`（去掉 `.example` 后缀）
3. 打开 `.env`，把内容替换成你的实际信息：

```
VITE_SUPABASE_URL=https://你的项目ID.supabase.co
VITE_SUPABASE_ANON_KEY=你的anon公开密钥
```

例如：
```
VITE_SUPABASE_URL=https://abcdefghijklmnopqrstuvwxyz.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3Mi...
```

4. 保存文件
5. **重启开发服务器**（在终端按 Ctrl+C 停止，然后重新运行 `npm run dev`）

> ⚠️ 配置完 .env 后必须重启服务器，否则不生效

---

## 第 7 步：注册第一个账号

1. 打开浏览器访问 http://localhost:5173/
2. 点击 **「没有账号？立即注册」**
3. 填写：
   - 姓名：你的名字（如：张三）
   - 邮箱：你的邮箱
   - 密码：至少 6 位
4. 点击 **「注册」**
5. 注册成功后：
   - **如果你关闭了邮箱验证**：可以直接登录使用
   - **如果开启了邮箱验证**：去邮箱点击验证链接，再回来登录

### 关闭邮箱验证（推荐小团队使用）

为了简化使用，可以关闭邮箱验证：

1. Supabase Dashboard → **Authentication** → **Providers**
2. 点击 **Email**
3. 关闭 **「Confirm email」** 开关
4. 点击 **「Save」**

这样注册后就能直接登录，不用邮箱验证。

---

## 第 8 步：把自己提升为管理员

新注册的用户默认是 **staff（员工）** 角色，没有权限管理产品和仓库。需要手动提升为 admin。

### 方法：在 SQL Editor 执行

1. 打开 Supabase Dashboard → **SQL Editor**
2. 新建查询，粘贴以下内容（**把邮箱换成你注册的邮箱**）：

```sql
-- 把你的账号提升为管理员
update public.profiles
set role = 'admin'
where id = (
  select id from auth.users where email = '你的邮箱@example.com'
);

-- 查看结果
select email, role from auth.users u
join public.profiles p on u.id = p.id;
```

3. 点击 **「Run」** 执行
4. 下方结果会显示你的邮箱和角色，应该看到 `admin`

5. 回到网页，**刷新页面**（或退出重新登录），就能看到管理员菜单了

---

## 第 9 步：开始使用系统

现在你可以完整使用系统了！建议按以下顺序测试：

### 9.1 添加产品
1. 点击左侧菜单 **「产品管理」**
2. 点击 **「新增产品」**
3. 填写：SKU（如 P001）、名称、条形码、分类、规格、单位
4. 上传一张产品图片
5. 点击 **「创建产品」**

### 9.2 添加仓库和库位
1. 点击左侧菜单 **「仓库管理」**
2. 新增仓库：编码 WH001、名称「主仓库」
3. 点击该仓库，在右侧新增库位：编码 `A-01-01`、描述「A区1排1层」
4. 可以多加几个库位

### 9.3 入库
1. 点击左侧菜单 **「入库」**
2. 点击 **「手动选择」** 或 **「扫码选择」**（如果有摄像头）
3. 选择刚才添加的产品
4. 选择仓库和库位
5. 输入数量（如 100）
6. 点击 **「确认入库」**

### 9.4 查看库存
1. 点击左侧菜单 **「库存查询」**
2. 可以看到刚才入库的产品和数量
3. 可以按产品名/SKU/条形码搜索，或按仓库筛选

### 9.5 出库
1. 点击左侧菜单 **「出库」**
2. 选择产品（会自动显示当前库存）
3. 选择要出库的库位
4. 输入出库数量
5. 点击 **「确认出库」**

### 9.6 查看记录
1. 点击左侧菜单 **「进出库记录」**
2. 可以看到所有进出库操作历史
3. 可以按入库/出库筛选，可以导出 CSV

---

## 常见问题排查

### Q1: 页面打开是空白的
- 检查 `.env` 文件是否配置正确
- 检查是否重启了开发服务器
- 按 F12 打开浏览器控制台查看错误信息

### Q2: 提示 "Missing authorization" 或 "Permission denied"
- 你还没有登录，先注册并登录
- 或者你不是管理员，但访问了管理员功能，需要先执行第 8 步提升为 admin

### Q3: 产品图片上传失败
- 检查 Storage 是否创建了 `product-images` 桶
- 检查桶是否设置为 **Public**
- 检查 SQL 文件是否都执行成功

### Q4: 入库/出库报错
- 确认两个 SQL 文件都执行成功
- 确认产品已经添加了条形码（扫码入库需要）
- 查看浏览器控制台的具体错误信息

### Q5: 扫码功能不工作
- 手机端：需要在 **HTTPS** 环境下才能使用摄像头（localhost 也可以）
- 手机端：浏览器需要授权摄像头权限
- 电脑端扫码枪：确保光标不在输入框内，否则会输入到框中

### Q6: 想添加第二个管理员
在 SQL Editor 执行（替换邮箱）：
```sql
update public.profiles
set role = 'admin'
where id = (
  select id from auth.users where email = '第二个管理员的邮箱'
);
```

### Q7: 想删除某个用户
```sql
-- 从 auth.users 删除（profiles 会自动联动删除）
delete from auth.users where email = '要删除的邮箱';
```

---

## 完成后的下一步

配置完成后，如果一切正常，可以继续开发：
- 阶段 4：PWA 配置 + 移动端适配（让手机端像 App 一样使用）
- 阶段 5：用户管理页面 + 工作台统计 + 部署上线

遇到任何问题，直接告诉我具体的错误信息，我会帮你排查。
