# 进出库管理系统 设计方案（v2 最终版）

> 仅供个人及小范围员工使用，纯网页形态，多设备链接分享即可访问。

---

## 一、项目概述

| 项 | 说明 |
|---|---|
| 目标用户 | 管理员 2 名 + 员工 5 名以下（共 ≤7 账号） |
| 产品规模 | 约 1000 种产品 |
| 双端形态 | 纯 Web 应用（一套代码响应式适配手机+电脑） |
| 手机端 | PWA（可"添加到主屏幕"像 App 一样使用，能唤起后置摄像头扫码） |
| 电脑端 | 浏览器打开网址即用，左侧导航 + 表格操作 |
| 核心能力 | 手动进出库、扫码进出库、产品图片/名称/仓库位置展示、库存查询 |
| 后端方案 | Supabase 云托管（PostgreSQL + Auth + Storage + Realtime） |
| 分发方式 | 部署后分享链接，多设备直接访问，无需安装 |

---

## 二、技术栈选型

### 前端（一套代码双端共用）
- **框架**：React 18 + TypeScript
- **构建**：Vite
- **UI**：Tailwind CSS + shadcn/ui
- **状态**：Zustand（轻量）+ React Query（服务端数据缓存）
- **路由**：React Router
- **PWA**：vite-plugin-pwa（manifest + Service Worker）

### 后端（Supabase 云托管）
- **数据库**：PostgreSQL
- **认证**：Supabase Auth（邮箱/密码）
- **文件存储**：Supabase Storage（产品图片）
- **实时**：Supabase Realtime（库存变化推送）
- **行级安全**：RLS 策略控制权限

### 扫码方案
| 场景 | 方案 |
|---|---|
| 手机端 | 浏览器 `getUserMedia` + `@zxing/browser`，调用后置摄像头实时识别条形码（EAN-13/Code128 等），识别成功震动反馈 |
| 电脑端 | USB 扫码枪（HID 键盘模式），前端监听 `keydown` 时间窗口组合条码，遇回车触发。无需驱动 |

---

## 三、功能模块

### 3.1 产品管理
- 新增/编辑/删除产品
- 字段：SKU、名称、条形码、分类、产品图片、规格、单位、描述
- 图片上传至 Supabase Storage
- 按条形码/SKU/名称搜索（1000 种规模无需复杂索引）

### 3.2 仓库与库位管理
- 仓库：名称、编码、地址
- 库位：归属仓库 + 库位编码（如 `A-01-03`）
- 一个产品可分布在多个库位

### 3.3 入库
- **手动入库**：选产品 + 选库位 + 输数量 + 批次(可选) + 备注 → 提交
- **扫码入库**：扫条码 → 自动带出产品 → 输数量/库位 → 提交
- 入库后自动增加库存

### 3.4 出库
- **手动出库**：选产品 + 库位 + 数量 → 提交（校验库存充足）
- **扫码出库**：扫条码 → 显示当前库存与位置 → 输数量 → 提交
- 库存不足时拦截

### 3.5 库存查询
- 按产品/仓库/库位筛选
- 列表展示：产品图片、名称、SKU、仓库、库位、可用库存
- 详情页：库位分布 + 最近进出库记录

### 3.6 进出库记录
- 时间倒序列表：时间、产品、类型(入/出)、数量、操作人、扫码/手动、批次、备注
- 按时间/产品/操作人筛选
- 导出 CSV

### 3.7 用户与权限
- 角色：管理员（admin）/ 员工（staff）
- 管理员：全部权限（管理产品、仓库、用户、查所有记录）
- 员工：进出库、查库存、查自己的记录
- 基于 Supabase Auth + RLS

---

## 四、数据库设计（PostgreSQL）

```sql
-- 产品
create table products (
  id uuid primary key default gen_random_uuid(),
  sku text unique not null,
  name text not null,
  barcode text,
  category text,
  spec text,
  unit text default '个',
  image_path text,
  description text,
  created_at timestamptz default now(),
  updated_at timestamptz default now()
);

-- 仓库
create table warehouses (
  id uuid primary key default gen_random_uuid(),
  code text unique not null,
  name text not null,
  address text,
  created_at timestamptz default now()
);

-- 库位
create table locations (
  id uuid primary key default gen_random_uuid(),
  warehouse_id uuid references warehouses(id) on delete cascade,
  code text not null,
  description text,
  unique (warehouse_id, code)
);

-- 库存（按产品+库位维度）
create table inventory (
  id uuid primary key default gen_random_uuid(),
  product_id uuid references products(id) on delete cascade,
  location_id uuid references locations(id) on delete restrict,
  quantity numeric(12,2) not null default 0,
  batch_no text,
  updated_at timestamptz default now(),
  unique (product_id, location_id)
);

-- 进出库流水
create table stock_moves (
  id uuid primary key default gen_random_uuid(),
  product_id uuid references products(id) on delete restrict,
  location_id uuid references locations(id) on delete restrict,
  move_type text not null check (move_type in ('in','out')),
  quantity numeric(12,2) not null,
  scan_mode text not null check (scan_mode in ('manual','scan')),
  batch_no text,
  remark text,
  operator_id uuid references auth.users,
  created_at timestamptz default now()
);

-- 用户扩展信息
create table profiles (
  id uuid primary key references auth.users(id) on delete cascade,
  name text,
  role text not null default 'staff' check (role in ('admin','staff')),
  created_at timestamptz default now()
);
```

### 关键流程（事务）
- **入库**：`insert stock_moves(in) + upsert inventory(+=qty)` 一个事务
- **出库**：校验 `inventory.quantity >= qty` → `insert stock_moves(out) + update inventory(-=qty)` 一个事务

---

## 五、页面结构

### 手机端（PWA，底部 Tab）
```
├─ 首页（快捷入口：扫码入库 / 扫码出库 / 查库存）
├─ 扫码（入库/出库切换，调用后置摄像头）
├─ 库存（搜索 + 列表，展示图片/名称/位置）
├─ 记录（最近进出库）
└─ 我的（账号 / 退出）
```

### 电脑端（Web，左侧导航）
```
├─ 工作台（库存概览/统计卡片）
├─ 产品管理
├─ 仓库管理（仓库 + 库位）
├─ 入库（手动 / 扫码枪）
├─ 出库（手动 / 扫码枪）
├─ 库存查询
├─ 进出库记录
├─ 用户管理（admin）
└─ 设置
```

---

## 六、项目结构

```
inventory-app/
├─ src/
│  ├─ components/            # 通用组件
│  │  ├─ ui/                 # 基础组件（Button/Input/Dialog等）
│  │  ├─ ProductPicker/      # 产品选择器
│  │  └─ Scanner/            # 扫码组件（摄像头 + 扫码枪）
│  ├─ pages/
│  │  ├─ mobile/             # 手机端页面（底部 Tab 布局）
│  │  ├─ desktop/            # 电脑端页面（侧边栏布局）
│  │  └─ auth/               # 登录页
│  ├─ hooks/
│  │  ├─ useScanner.ts       # 摄像头扫码 Hook
│  │  ├─ useBarcodeGun.ts    # 扫码枪监听 Hook
│  │  └─ useDevice.ts        # 设备类型判断
│  ├─ lib/
│  │  ├─ supabase.ts         # Supabase 客户端
│  │  ├─ db.ts               # 数据库操作封装
│  │  └─ utils.ts
│  ├─ types/
│  └─ App.tsx                # 根据设备类型路由到 mobile/desktop
├─ public/
│  ├─ manifest.json          # PWA 配置
│  └─ icons/                 # PWA 图标
├─ supabase/
│  ├─ migrations/            # 数据库迁移 SQL
│  └─ policies.sql           # RLS 策略
├─ vite.config.ts
├─ tailwind.config.js
└─ package.json
```

---

## 七、开发里程碑

| 阶段 | 内容 | 产出 |
|---|---|---|
| 1 | 项目初始化 + Supabase 建表 + 登录 + 产品 CRUD + 图片上传 | 可管理产品 |
| 2 | 仓库/库位管理 + 手动进出库 + 库存查询 | 核心流程跑通 |
| 3 | 扫码功能（手机摄像头 + 电脑扫码枪监听） | 扫码进出库可用 |
| 4 | PWA 配置 + 移动端响应式适配 + 双端布局 | 双端可用 |
| 5 | 用户权限(RLS) + 进出库记录 + CSV 导出 + 工作台统计 | 完整可用 |

---

## 八、关键技术点

### 8.1 设备类型路由
```ts
// 根据屏幕宽度或 UA 判断设备，路由到对应布局
const isMobile = useMediaQuery('(max-width: 768px)');
// 或通过 URL 显式区分：/m/* 手机端，/d/* 电脑端
```

### 8.2 手机摄像头扫码
使用 `@zxing/browser` 调用后置摄像头，识别 EAN-13/Code128 等条形码，识别成功后震动反馈并自动填充。

### 8.3 电脑扫码枪实现
USB 扫码枪默认为键盘模拟模式，扫码后自动输入字符 + 回车。前端无需驱动：
```ts
let buffer = '';
let lastTime = 0;
window.addEventListener('keydown', (e) => {
  const now = Date.now();
  if (now - lastTime > 100) buffer = '';   // 超时重置
  lastTime = now;
  if (e.key === 'Enter') { handleBarcode(buffer); buffer = ''; }
  else if (e.key.length === 1) buffer += e.key;
});
```

### 8.4 数据安全
- 所有表启用 RLS：员工只能看自己的操作记录，管理员可看全部
- 产品/库存数据按需开放读写权限
- 客户端用 anon key + RLS，service key 仅服务端

---

## 九、部署与分发

- **应用部署**：Vite 构建后部署到 Vercel / Netlify / Cloudflare Pages（免费额度够用）
- **后端**：Supabase 云托管，无需自建服务器
- **分发方式**：
  - 电脑端：员工浏览器打开网址即可，可收藏书签
  - 手机端：浏览器打开 → 浏览器菜单 → "添加到主屏幕"，桌面出现 App 图标
  - 多设备分享：直接发链接，账号登录即可使用
